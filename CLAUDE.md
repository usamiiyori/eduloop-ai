# CLAUDE.md — EduLoop AI

このファイルは、このリポジトリで作業する Claude Code（および他の開発者）が守るべき規約・原則を定義する。実装前に必ず読むこと。
設計根拠の詳細は `docs/ARCHITECTURE.md`、フェーズ計画と進捗は `docs/ROADMAP.md`、非エンジニア向け運用手順は `docs/運用マニュアル.md` を参照。

## 0. 絶対ルール（最優先・全フェーズ共通・違反禁止）

このプロジェクトのオーナーは**現場の教育関係者であり、プログラマではない**。コードを読んで自分で直すことはできない。この前提をすべての判断基準にする。

1. **フェーズごとに必ず停止する。** `docs/ROADMAP.md` の Phase が完了したら、次の Phase に勝手に進んではならない。停止し、本ファイル末尾の「Phase報告フォーマット」で日本語報告を出し、オーナーの指示を待つ。
2. **オーナーにトレースバックを読ませない。** すべてのエラーは日本語に変換し、「何が起きたか / なぜか / オーナーが取るべき操作は何か」の3点に整形して提示する。
3. **オーナーに手作業を要求する場合は、画面名・ボタン名まで書いた手順書を用意する。** 「適宜設定してください」は禁止。
4. **すべての操作をワンコマンド化する。** `make setup` / `make run` / `make doctor` / `make test` / `make cost` / `make stop` のように、覚えるコマンドは5〜6個以内に収める。
5. **推測でコードを書かない。** 外部サービス仕様（API料金体系・レート制限・利用規約・スキーマ）が不確実な場合、実装前に公式ドキュメントを確認する。確認できなければ「未確認」と明記し、オーナーに判断を仰ぐ。
6. **秘密情報をコードに書かない。** すべて環境変数 + `.env.example` 経由（詳細は第7章）。
7. **各 Phase の成果物には必ず動作証跡を添える。** 「実装しました」ではなく、実際に動かした出力・実データ・テスト結果を示す。

速度よりも正確性を優先する。オーナーは実名で発信するため、**判断に迷ったら「出さない」を選ぶ**。この方針は第2章のハーネス層（特にG3/G4）に実装として反映されている。

## 1. プロダクト概要

**EduLoop AI** は「教育 × 生成AI」領域における公的機関・学術機関の一次情報を自律収集し、事実整合性を検証したうえで教育現場（教員）向けに配信し、将来的には教員コミュニティの実践知を集約して収集・配信の質に還元する、Human-on-the-Loop 型の自律配信プラットフォームである。

**核心原則: Human-on-the-Loop。** AIは収集・要約・下書き生成・自動検証までを担うが、外部への配信（公開）は人間の承認ゲート（L2）を経るまで実行してはならない。自律性は「準備」までであり、「公開」の最終責任は人間にある。

**勝ち筋（この2点以外で差別化しない）**: ①速報性（一次情報公開当日に論点を出す）②現場翻訳（「この答申は明日の校務にこう効く」という接続。元官僚でもAI研究者でも書けない現場人間の独占領域）。

## 2. アーキテクチャ原則（4層 + 6ゲート）

```
[1. プロンプト層]   一次情報 → 論点抽出（Fact / Implication / Discussion / Citation）
        ↓
[2. コンテキスト層] 学習指導要領・過去答申・自治体方針・編集ペルソナ・教員フィードバックの動的注入
        ↓
[3. ハーネス層]     G1〜G6 の自動検証ゲート + 自己修復ループ（最大3回リトライ）
        ↓
[4. ループ層]       L1 自動収集(毎時) → L2 承認(日次・人間) → L3 知見還元(月次)
```

1. **プロンプト層**: Fact / Implication / Discussion / Citation の4観点で論点を抽出する。抽出物は必ず原文に紐づく。数字は原文にない概算・丸めを禁止。
2. **コンテキスト層**: 学習指導要領・過去答申・自治体方針・教員フィードバック・編集ペルソナ（`config/editorial_voice.yaml`）を動的注入する。ハードコードせず、Supabase 等から動的取得する。
3. **ハーネス層**: `src/harness/` に実装する。以下6ゲートを**全パス**しない生成物は次フェーズに進めてはならない。
   - **G1 事実整合性**: 抽出した事実主張・数値が原文に実在するか（文字列一致 + 埋め込み類似度のハイブリッド照合）
   - **G2 出典・ライセンス**: SIST02 準拠の書誌情報 + Schema.org（NewsArticle/TechArticle）準拠。`sources.yaml` の再配布モード（第4章）に違反していないか
   - **G3 個人情報 (PII)**: 生徒名・学校名・教員個人名等を正規表現＋モデレーションAPIで検出。検出時は自動マスクせず**ブロックして人間に回す**
   - **G4 断定リスク（最重要）**: 政策文書の段階（検討中/中間まとめ/答申/告示/施行）を誤読していないか、原文で裏付けられない断定（「〜が決定した」等）をしていないかを LLM-as-a-Judge で判定
   - **G5 重複・既報**: 過去出力との埋め込みコサイン類似度が閾値超過なら「続報」フォーマットへ強制切替
   - **G6 構造**: Web記事Markdown / Xスレッド / YouTube台本の Pydantic v2 スキーマに準拠しているか
   - 失敗時はエラーログを添えて**最大3回まで**自律リトライする。3回失敗したら `needs_human` ステータスで人間のレビューキューに送る（黙って捨てない）。
4. **ループ層**: L1（高速自動・数十分〜1時間間隔の収集）→ L2（人間の監督/承認・日次、Slack Webhookで通知＋Web承認画面で操作）→ L3（知見還元・月次）。L2 を経ないコンテンツは公開 DB・配信チャネルに触れてはならない。

## 3. ディレクトリ構成と責務

```
src/
  scrapers/     # RSS/Atom, HTML差分検知, PDF抽出の収集モジュール（副作用: 外部I/Oのみ、加工しない）
  processors/   # プロンプト層・コンテキスト層。生データ→構造化ドラフトへの変換
  harness/      # G1〜G6 検証ゲート + 自己修復リトライ
  publishers/   # L2承認後のみ呼び出される配信モジュール（Web/X/note/YouTube等）。承認フラグ未確認での実行を禁止
  models/       # Pydantic v2 モデル（一次情報, 引用メタデータ, 教員コメント, 監査ログ, メトリクス）
config/
  sources.yaml          # 収集ソースレジストリ + ライセンス台帳（第4章）。コード直書き禁止、YAML追記のみでソース追加できること
  editorial_voice.yaml  # 編集ペルソナ・文体規定（第5章）。オーナーがコードを触らず調整できること
tests/          # pytest。harness の6ゲートは必ずユニットテストで裏付ける
sql/            # Supabase スキーマ・RLSポリシー（番号付きマイグレーション、追記のみ）
docs/           # 設計ドキュメント・ロードマップ・運用マニュアル
.github/workflows/  # GitHub Actions cron（L1毎時 / L2日次 / L3月次）
```

依存方向は `scrapers → processors → harness → publishers` の一方向。`publishers` が `scrapers` を直接呼ぶなど層を飛び越える依存は禁止。

## 4. 収集ソースと法務ゲート

収集対象ソース一覧（行政・政策/地域・自治体/世界・学術・技術の3軸）は `docs/ARCHITECTURE.md` を参照。ソースは `config/sources.yaml` のレジストリで管理し、**コード直書きしない**。オーナーがYAMLに1行足すだけでソースを追加できる構造にする。

**このシステムは著作権リスクを抱える構造である**（文化庁著作権WG資料等をスクレイピングするため）。`sources.yaml` の各ソースには以下を必須フィールドとする。

```yaml
- id: mext_press
  name: 文部科学省 報道発表
  license: gov_standard_terms_2_0   # 政府標準利用規約(第2.0版) / CC-BY-4.0 / arxiv-varies / copyrighted-quote-only
  attribution_required: true
  redistribution: full_allowed      # full_allowed / summary_only / quote_only
  quote_max_ratio: null             # quote_only の場合は 0.1 など
```

再配布モードごとの生成ルールは G2 ゲートで強制する: `full_allowed`=本文引用可（出典明記必須）/ `summary_only`=逐語引用禁止・要約のみ / `quote_only`=指定比率以下・著作権法32条の引用要件（主従関係・明瞭区別・必然性・出典明示）を満たす構造で出力。arXiv は論文ごとにライセンスが異なるため論文単位で取得し、CC BY-NC-ND は図表転載を禁止する分岐を実装する。

> 注: これは設計上のリスク管理であり法的助言ではない。収益化フェーズに入る前に専門家への確認を検討すべき旨を `docs/運用マニュアル.md` に明記する。

## 5. 編集ペルソナ・文体規定

`config/editorial_voice.yaml` に外出しし、オーナーがコードを触らず調整できるようにする。初期値: 一人称は現場の教育関係者（評論家ではない）/ 禁止表現（「いかがでしたか」等のAI的定型句、生成後に禁止語リストで機械チェック）/ 必須構造（①事実②現場への含意③まだ決まっていないこと④出典の4部構成）/ 数字は原文にない概算・丸め禁止 / 政策への賛否は論点提示までとし断定的な政治的主張はしない。

## 6. コーディング規約

- Python 3.11+ / 型ヒント必須（`from __future__ import annotations` を各モジュール冒頭に）。
- データ構造は Pydantic v2 (`BaseModel` / `model_validate` / `model_dump`) で定義し、dict を生で使い回さない。
- 非同期I/O（スクレイピング・API呼び出し）は `async def` + `httpx.AsyncClient` を基本とする。Playwright は動的レンダリングが必要なページのみに限定し、静的HTML/RSSは `httpx` + `BeautifulSoup4` で軽量に処理する。
- 外部呼び出し（HTTP, LLM API, DB）には `tenacity` でリトライ（指数バックオフ）を必ず付与する。
- ログは標準 `logging` を `structlog` でラップした構造化ログを使用し、`print` は使わない。
- コメントは「なぜ」のみ。何をしているかは型と命名で伝える。
- 1関数1責務。スクレイパーは取得のみ、加工は processors、判定は harness に置く。

## 7. シークレット管理

- APIキー・DB接続情報を**コード内・リポジトリ内に絶対に含めない**。
- ローカルは `.env`（`.env.example` を参照して各自作成、`.gitignore` 済み）。
- 本番の実行基盤は **GitHub Actions**（第9章）。秘密情報は GitHub リポジトリの Settings → Secrets and variables → Actions に登録し、ワークフローの `env:` 経由で渡す。Google Secret Manager は使わない（オーナー単独で復旧できないため。Phase 6 で GCP 移行が必要になった場合のみ切替を検討する）。
- `src/config.py` の `Settings` 経由でのみ環境値を読む。各モジュールで `os.environ` を直接読まない。

## 8. データ基盤

- DB/認証: Supabase (PostgreSQL)。すべての公開系テーブルは RLS (Row Level Security) を有効化する。RLSを無効にしたままマイグレーションをコミットしない。
- スキーマ変更は `sql/` 配下に番号付きマイグレーションファイルとして追加し、既存ファイルは変更しない（追記のみ）。
- 将来の教員コミュニティ機能（ログイン・コメント・実践知蓄積）を見越したテーブル設計を初期スキーマから含める。
- 収益計測は推測でなく計測可能な形でDBに持たせる: `post_metrics`（集客指標、手動入力とAPI取得の両対応）/ `link_clicks`（UTM経由の回遊）/ `conversions`（note等の収益、手動入力フォールバック必須）/ `inbound_leads`（登壇・問い合わせ等の信用指標）。詳細は `docs/ARCHITECTURE.md`。

## 9. 実行基盤・運用（ノーコード運用者向け）

- **スケジューラ・実行基盤は GitHub Actions（cron）を既定とする。** シークレット管理・実行ログ閲覧・失敗時の再実行がすべてブラウザUIで完結し、オーナー単独で運用できるため。L1毎時 / L2日次 / L3月次のワークフローを `.github/workflows/` に置く。実行時間15分超・常時起動・大量並列が必要になった時点でのみ GCP 移行を検討する（判断基準は `docs/運用マニュアル.md` に明記）。
- **承認インターフェース**: Slack Webhook（オーナー選定、Discordは不使用）で「レビュー依頼」を通知する。実際の「承認/却下/修正指示」操作はWeb承認画面（Supabase連携）で行う。理由: SlackのIncoming Webhookは送信専用で、ボタン操作を受け取るには常時稼働サーバー（Interactivity機能）が必要になり、GitHub Actionsのみで完結する運用方針（第9章）と相性が悪いため、通知と操作を分離した（2026-08-22確認・オーナー承認済み）。
- **`make doctor`**: APIキーの有無・DB接続・各ソースの疎通・直近の実行結果を診断し、日本語で「正常/異常＋対処法」を一覧表示する。
- **`make cost`**: LLM APIの当月推定コストを日本語で表示する。日次コスト上限を超過した場合は自動停止し Slack に通知する。
- **`make stop`**: キルスイッチ。全自動処理を即停止できる。
- 通知は成功/失敗/要承認/コスト超過のすべてを Slack に日本語で送る。

## 10. テスト方針

- `harness/` の6ゲート（G1〜G6）は実装と同時にユニットテストを書く（テストなしのハーネスコードはマージしない）。
- スクレイパーは実サイトへの実リクエストをテストで行わない。固定HTML/PDFフィクスチャを `tests/fixtures/` に置いてパースロジックのみ検証する。
- PII検出（G3）・事実整合性（G1）・断定リスク（G4）は、意図的に「壊れた」入力（PII混入、原文にない数値、原文で裏付けられない断定表現）を与えて**検出できること**を確認するテストを必須で用意する。
- `pytest` をデフォルトランナーとし、CIでは `ruff check` と `mypy` も必須ゲートとする。
- robots.txt 尊重・クロール間隔（最低3秒）は `scrapers` のテストで担保する。

## 11. 禁止事項

- L2承認前のコンテンツを `publishers/` から配信すること。
- 生徒・教員個人が特定される情報を含んだままの生成物を harness（G3）を通さずに次フェーズへ進めること。
- 原文で裏付けられない断定表現・政策段階の誤読を harness（G4）を通さずに配信すること。
- `sources.yaml` の再配布モード（`quote_only` の引用比率等）に違反した配信。
- 収集元サイトの利用規約・robots.txt を無視したクローリング（レート制御なしの高頻度アクセスを含む）。
- シークレットのハードコード、または `.env` のコミット。
- オーナーに生のトレースバックを見せること、または「適宜設定してください」のような丸投げの手順書を渡すこと。
- 現在の Phase が完了する前に次の Phase の実装に着手すること（第0章ルール1）。

## Phase報告フォーマット

各Phase終了時に必ずこの形式で日本語報告する。

```markdown
## Phase N 完了報告

### 作ったもの
（非エンジニアに伝わる言葉で3〜5行）

### 動作確認の証跡
（実際のコマンド出力・実データ・テスト結果）

### オーナーに今やってほしい操作
（ない場合は「なし」。ある場合は画面名とボタン名まで具体的に）

### 判断を仰ぎたいこと
（設計上の分岐で、オーナーが決めるべきこと）

### 未確認・リスク
（外部仕様で確認できなかったこと、将来の懸念）

### 次のPhaseでやること
（3行程度）
```
